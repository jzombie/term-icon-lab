[package]
name = "icons"
version = "0.1.0"
edition = "2024"
publish = false
description = "Browse and search the term-icon verified glyph set."

[dependencies]
term-icon = { path = "../.." }
clap = { workspace = true }
